
Процедура ДобавитьЭлемент(Данные, ТекстСообщения) Экспорт
	
	НовыйЭлемент = Справочники.ПолученныеСообщения.СоздатьЭлемент();
	ЗаполнитьЗначенияСвойств(НовыйЭлемент, Данные);
	НовыйЭлемент.ТекстСообщения = Новый ХранилищеЗначения(ТекстСообщения, Новый СжатиеДанных(9));
	
	НовыйЭлемент.Записать();
	
КонецПроцедуры	